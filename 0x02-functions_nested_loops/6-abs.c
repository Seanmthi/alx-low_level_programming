#include "main.h"
/**
 * _abs - determines the absolute value
 *
 * @n: integer
 *
 * Return: the absolute value
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
